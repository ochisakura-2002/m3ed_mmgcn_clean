# Project Map Compatibility Notice

此文件已停止作为项目主上下文维护。

唯一的详细项目上下文是：

- `docs/PROJECT_CONTEXT.md`

Codex 操作规则和任务阅读入口位于：

- `AGENTS.md`

保留 `docs/project_map.md` 路径，仅用于兼容历史文档和旧任务中的链接。
读取到本页后，应转到 `AGENTS.md` 和 `docs/PROJECT_CONTEXT.md` 的相关章节。

不要在本文件继续增加或维护以下内容：

- 模型与复现忠实度信息
- 配置、入口脚本或目录清单
- 数据、特征与实验协议信息
- 当前实验状态或历史决策

新增模型、协议、数据版本、输出规则或论文主线等重大变化时，
只更新 `docs/PROJECT_CONTEXT.md`，不要创建第二份项目总结。
